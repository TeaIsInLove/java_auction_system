name: CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build and run tests
        # Override compiler source/target to 21 so CI (JDK 21) can compile the project
        run: mvn -B clean test -Djava.version=21 --file pom.xml
        env:
          # Suppress JavaFX headless errors in CI — tests do not use JavaFX
          JAVA_TOOL_OPTIONS: "-Djava.awt.headless=true"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: target/surefire-reports/
